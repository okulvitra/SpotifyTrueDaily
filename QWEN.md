# SpotifyTrueDaily Project Context for Qwen Code

## Project Overview

SpotifyTrueDaily is a Python application that automatically creates and updates a daily personalized Spotify playlist named "Journalière". It combines various content sources:

1.  **Podcasts:** The latest unlistened episodes from saved shows, filtering out audiobooks.
2.  **Music:**
    *   Recently played tracks.
    *   Top tracks (short-term).
    *   Random tracks from the user's library.
    *   (Optional) New music recommendations using the SoundStat API, seeded by recent/top tracks.
3.  **Interleaving:** The collected content is intelligently interleaved (e.g., 1 podcast episode followed by 3 songs) to create a varied listening experience.

The application is designed for daily use, either manually or automated via Windows Task Scheduler. It features multiple user interfaces built with different frameworks for different user preferences.

## Core Technologies

*   **Language:** Python 3.x
*   **Spotify Interaction:** `spotipy` library
*   **API Calls (SoundStat):** `requests` library
*   **Packaging (Optional):** `PyInstaller` (for creating `.exe`)
*   **Dependencies:** Listed in `requirements.txt` and `kivy_requirements.txt`.

## Project Structure

### Main Application Files
*   `playlist_manager.py`: The core logic script. It manages Spotify authentication (`spotipy`), fetches content (recent, top, podcasts, library, SoundStat), deduplicates and interleaves the content, and finally updates the "Journalière" playlist on Spotify.
*   `config_template.py`: A template file that users must copy to `config.py` and fill in with their Spotify API credentials, SoundStat API key (optional), and default content parameters.
*   `config.py` *(Not versioned)*: The local configuration file generated by the GUI or manually created. Contains sensitive API keys and user preferences. This file is loaded by `playlist_manager.py`.
*   `requirements.txt`: Lists the Python packages required for the main application.
*   `.spotipyoauthcache` *(Not versioned)*: Cache file created by `spotipy` to store and reuse Spotify OAuth tokens after the initial manual authentication.

### Modern CustomTkinter UI (Primary UI)
*   `app_modern.py`: The main script for the CustomTkinter GUI application. It provides a modern interface with sidebar navigation, configuration cards, and integrated logging.
*   `modern_ui.py`: Contains modern UI components like themes, buttons, sidebar, and cards.
*   `TODO.md`: Tracks ongoing UI development tasks and improvements.

### Kivy UI (Alternative UI)
*   `kivy_ui/`: Main directory for Kivy UI components
  - `main_app.py`: Main Kivy application class
  - `screens/`: Directory for different app screens (dashboard, settings, playlist_config)
 - `components/`: Reusable UI components (buttons, cards, navbar, stepper)
  - `themes/`: Theme definitions and styling
  - `utils/`: Utility functions and helpers
*   `kivy_requirements.txt`: Dependencies specific to Kivy implementation

### Documentation and Utilities
*   `README.md`: Main project documentation with installation and usage instructions.
*   `README_MODERN.md`: Documentation for the modern CustomTkinter UI.
*   `README_KIVY.md`: Documentation for the Kivy UI implementation.
*   `MIGRATION_GUIDE.md`: Guide for transitioning from the original application to the modern version.
*   `LUCIDE_ICONS_GUIDE.md`: Guide for using Lucide icons in the application.
*   `run_playlist_update.bat`: A sample batch script for running the Python script version from the command line or Task Scheduler.
*   `SpotifyTrueDaily.exe` *(Not versioned, in releases)*: The standalone executable built with PyInstaller.
*   `SpotifyTrueDaily.spec`: The PyInstaller spec file used for building the executable.

## Key Features and Logic

*   **Spotify Authentication:** Uses `spotipy`'s `SpotifyOAuth` with a local cache file (`.spotipyoauthcache`) for seamless subsequent logins after the initial browser-based authorization.
*   **Playlist Management:** Checks for an existing "Journalière" playlist; if found, it uses it. If not, it creates a new private playlist.
*   **Podcast Handling:** Fetches saved shows, retrieves the latest episode for each, filters out episodes marked as fully played or identified as audiobook chapters, and selects up to the configured maximum number of unlistened episodes.
*   **Music Sources:**
    *   Queries Spotify for recently played and top tracks based on configured limits.
    *   Selects random tracks from the user's saved tracks, ensuring they are not already included from recent/top lists.
*   **SoundStat Integration (Optional):** If a SoundStat API key is provided and the relevant counts are > 0:
    *   Seeds are selected from recent and top tracks.
    *   Makes API calls to SoundStat for recommendations based on these seeds.
    *   Filters SoundStat recommendations to remove duplicates and items already present from other sources.
    *   Selects up to the configured maximum number of unique new recommendations.
*   **Content Interleaving:** Combines podcast URIs and music URIs (recent, top, library, SoundStat) into a single list, ensuring uniqueness. It then creates a new list by interleaving them (1 podcast, 3 songs, repeat) to produce the final playlist order.
*   **Playlist Update:** Clears the existing playlist content and adds the newly generated interleaved list of track/episode URIs.

## Multiple UI Implementations

### Modern CustomTkinter UI (`app_modern.py`)
*   **Modern Interface:** Clean, Spotify-inspired dark theme with blue/violet accents
*   **Sidebar Navigation:** Easy access to Dashboard, Playlist Configuration, Settings, and About sections
*   **Configuration Cards:** Intuitive interface for adjusting content parameters with stepper controls
*   **Integrated Logging:** Real-time display of application logs and status updates
*   **Responsive Design:** Adapts to different screen sizes and window states

### Kivy UI (`kivy_ui/`)
*   **Cross-Platform:** Built with Kivy for potential mobile and web deployment
*   **Component-Based Architecture:** Modular design with reusable UI components
*   **Modern Design Principles:** Follows 2025 Kivy best practices
*   **Screen Management:** Uses Kivy's ScreenManager for navigation between different views

## Building and Running

### For Developers (Python Scripts)

1.  **Clone Repository:**
    ```bash
    git clone https://github.com/okulvitra/SpotifyTrueDaily.git
    cd SpotifyTrueDaily
    ```
2. **Set Up Virtual Environment (Recommended):**
    ```bash
    python -m venv .venv
    source .venv/bin/activate  # On Linux/macOS
    # .venv\Scripts\Activate.ps1 # On Windows (PowerShell)
    ```
3.  **Install Dependencies:**
    ```bash
    pip install -r requirements.txt
    ```
4.  **Configure API Keys:**
    *   Copy `config_template.py` to `config.py`.
    *   Edit `config.py` and enter your Spotify `Client ID`, `Client Secret`, `Redirect URI`, and optionally your SoundStat `API Key`. Set your default content parameters.
5.  **Run the Application:**
    *   Launch the modern GUI:
        ```bash
        python app_modern.py
        ```
    *   Or launch the Kivy GUI:
        ```bash
        pip install -r kivy_requirements.txt
        python kivy_ui/main_app.py
        ```
    *   Use the appropriate settings interface to save your configuration.
    *   Adjust parameters in the configuration interface if needed for this run.
    *   Click "Refresh Now" or equivalent. If it's the first run or the cache is invalid, follow the browser authentication flow and paste the redirect URL.

### For Users (Pre-built Executable - Windows)

1.  **Download:** Get `SpotifyTrueDaily.exe` and `config_template.py` from the latest release.
2.  **Setup:** Place both files in the same directory.
3.  **Configure:** Run `SpotifyTrueDaily.exe`, use the "Settings" or "API Settings" tab to enter your API keys and save the configuration (this creates `config.py`).
4.  **Authenticate (First Run):** Click "Refresh Now" or equivalent, follow the browser prompt, and paste the redirect URL.
5.  **Use:** Run `SpotifyTrueDaily.exe`, adjust parameters in the configuration interface, and click "Refresh Now".

## Development Conventions

*   **Configuration:** Centralized in `config.py`, loaded by `playlist_manager.py`. GUI dynamically updates `config.py`.
*   **Error Handling:** Specific exceptions (`ConfigError`) and general `try...except` blocks are used to catch errors during configuration loading, API calls, and GUI operations. Errors are displayed in the GUI or printed to the console.
*   **Logging/Output:** Standard `print` statements are used extensively for logging within `playlist_manager.py` and GUI applications. The GUIs provide integrated log displays.
*   **Threading:** The playlist update process is run in a separate thread in the GUIs to prevent the interface from freezing.
*   **API Limits:** Basic `time.sleep` calls are used between some Spotify API requests to be respectful of rate limits. SoundStat API calls also use specific limits per request.
*   **Deduplication:** Logic is present to prevent the same track/episode URI from appearing multiple times in the final playlist.

## Recent Development Updates

### Modern UI Improvements
*   **Theme Updates:** Modern dark theme with Spotify-inspired blue/violet accents
*   **Navigation Restructure:** Simplified sidebar navigation focusing on core functionality
*   **Configuration Cards:** Intuitive card-based interface for playlist configuration with stepper controls
*   **Real-time Updates:** Configuration changes are immediately reflected in the application

### Kivy UI Development
*   **Component Architecture:** Modular design with reusable buttons, cards, navbar, and stepper components
*   **Modern Styling:** Consistent color scheme and typography following 2025 best practices
*   **Responsive Layouts:** Adaptive designs that work across different screen sizes
*   **Screen Management:** Proper navigation between dashboard, settings, and configuration screens

### Bug Fixes and Enhancements
*   **Interface Fixes:** Resolved display and scrolling issues in Kivy screens
*   **Icon Display:** Fixed emoji icon rendering issues by using standard Unicode characters
*   **Layout Improvements:** Adjusted component heights and responsive behavior
*   **Stepper Component:** Replaced inconsistent slider with reliable stepper controls for numeric inputs
*   **Scrolling Behavior:** Fixed automatic scrolling and content positioning issues

## Future Development Roadmap

### UI/UX Improvements
*   **Color Scheme Updates:** Transition from Spotify green to dark blue/violet theme
*   **Navigation Simplification:** Streamline sidebar to focus on essential sections
*   **Enhanced Configuration:** Implement comprehensive playlist configuration cards
*   **Visual Polish:** Final consistency checks and responsive design improvements

### Feature Enhancements
*   **Advanced Analytics:** Integration of music listening statistics and insights
*   **Smart Recommendations:** Enhanced AI-driven playlist suggestions
*   **Cross-Platform Support:** Full mobile and web deployment of Kivy UI
*   **Performance Optimization:** Improved loading times and API efficiency
